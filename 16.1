#include <iostream>
#include <stack>
#include <string>

using namespace std;

int countAndRemoveWords( stack<string>& originalStack, char letter) {
    stack< string> tempStack;
    int count = 0;

    while (!originalStack.empty()) {
        string word = originalStack.top();
        originalStack.pop();

        if (word[0] == letter) {
            count++;
        } else {
            tempStack.push(word);
        }
    }

    while (!tempStack.empty()) {
    	originalStack.push(tempStack.top());
        tempStack.pop();
    }

    return count;
}

int main() {
    setlocale(LC_ALL,"Russian");
    stack< string> wordsStack;

    wordsStack.push("apple");
    wordsStack.push("banana");
    wordsStack.push("carrot");//
    wordsStack.push("cannon");//
    wordsStack.push("dog");
    wordsStack.push("elephant");
    wordsStack.push("fish");

    char letter = 'c';

    int count = countAndRemoveWords(wordsStack, letter);
    
    cout << "Êîë-âî ñëîâ ñ çàäàííîé áóêâû'" << letter << "': " << count <<  endl;
    cout << "Ñëîâà ïîñëå óäàëåíèÿ: " <<  endl;
    
    while (!wordsStack.empty()) {
       	cout << wordsStack.top() <<  endl;
        wordsStack.pop();
    }

    return 0;
}
