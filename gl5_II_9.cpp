#include <iostream>
#include <cmath>
using namespace std;

int main(){
    setlocale(LC_ALL,"Russian");

    int k, S=0, x;
    cout << "Введите k: ";
    cin >> k;
    
    for (int n=1; n<=k; ++n){
        n = 2*n;
        S += (-1*pow(x,n)/);
    }

    return 0;
}